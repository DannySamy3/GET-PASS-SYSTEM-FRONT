import React from "react";
import { StudentList } from "@/components/studentList/StudentList";

const page = () => {
  return (
    <div>
      <StudentList />
    </div>
  );
};

export default page;
